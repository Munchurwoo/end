<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="join.us.GoodJob.model.mapper.CompanyMapper">
  <insert id="insertCompanyMember" parameterType="companyMemberVO">
		insert into
		company_member(company_id,introduction,company_type,industry,sales,date_of_establishment,num_of_employees)
		values(company_id=#{id},#{introduction},#{company_type},#{industry},#{sales},#{date_of_establishment},#{num_of_employees})
	</insert>
<insert id="insertMember" parameterType="memberVO">
		insert into member(id,password,email,name,tel,member_type,address) 
		values(#{id}, #{password}, #{email}, #{name}, #{tel}, 3, #{address})
	</insert>
 </mapper> 		
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
















